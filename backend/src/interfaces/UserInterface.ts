export interface IUser {
    username: string;
    isAdmin: boolean;
    id: string;
}

export interface IDatabaseUser {
    username: string;
    password: string;
    email: string;
    isAdmin: boolean;
    id: string;
}